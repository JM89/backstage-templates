apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: ${{ values.name }}
  description: ${{ values.description }}
  annotations:
    github.com/project-slug: JM89/${{ values.name }}
    backstage.io/techdocs-ref: dir:.
  links:
  - url: https://dashboard.example.com/${{ values.name }}
    title: My Dashboard
    icon: dashboard
  tags:
    - domain-one
    - critical-path
    - framework-version
spec:
  type: service
  owner: jm89
  lifecycle: experimental
  dependsOn:
    - resource:JM89/test-distributed-tracing
    - component:demo
